rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Utilisateurs peuvent accéder à leurs propres données complètes
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
      
      // Permettre la lecture des données du partenaire connecté (HYBRIDE - TOUS LES CHAMPS)
      allow read: if request.auth != null && 
                     isConnectedPartnerHybrid(request.auth.uid, userId);
    }
    
    // Collection séparée pour les données partagées entre partenaires
    match /sharedPartnerData/{userId} {
      // L'utilisateur peut écrire ses propres données partagées
      allow write: if request.auth != null && request.auth.uid == userId;
      
      // Les partenaires connectés peuvent lire ces données partagées
      allow read: if request.auth != null && 
                     (request.auth.uid == userId || 
                      isConnectedPartnerHybrid(request.auth.uid, userId));
    }
    
    // Règles pour les codes partenaires
    match /partnerCodes/{codeId} {
      // Permettre la lecture pour vérifier l'existence et la validité d'un code
      allow read: if request.auth != null;
      
      // Permettre la création seulement par le propriétaire du code
      allow create: if request.auth != null && 
                   request.auth.uid == request.resource.data.userId;
      
      // Permettre la mise à jour dans ces cas :
      // 1. Le propriétaire du code peut toujours modifier son code
      // 2. Un utilisateur peut se connecter à un code libre (connectedPartnerId == null)
      // 3. Le partenaire connecté peut modifier certains champs
      allow update: if request.auth != null && 
                   (
                     // Cas 1: Propriétaire du code
                     request.auth.uid == resource.data.userId ||
                     
                     // Cas 2: Connexion à un code libre
                     (resource.data.connectedPartnerId == null && 
                      request.resource.data.connectedPartnerId == request.auth.uid) ||
                     
                     // Cas 3: Partenaire déjà connecté modifie ses données
                     (request.auth.uid == resource.data.connectedPartnerId)
                   );
      
      // Permettre la suppression seulement par le propriétaire
      allow delete: if request.auth != null && 
                   request.auth.uid == resource.data.userId;
    }
    
    // Règles pour les entrées de journal partagé
    match /journalEntries/{entryId} {
      // Lecture : utilisateur authentifié ET (auteur OU dans partnerIds)
      allow read: if request.auth != null && 
                     (request.auth.uid == resource.data.authorId ||
                      (resource.data.partnerIds != null && 
                       request.auth.uid in resource.data.partnerIds));
      
      // Création : utilisateur authentifié ET auteur ET dans partnerIds
      allow create: if request.auth != null && 
                       request.auth.uid == request.resource.data.authorId &&
                       request.resource.data.partnerIds != null &&
                       request.auth.uid in request.resource.data.partnerIds;
      
      // Modification : seulement l'auteur peut modifier
      allow update: if request.auth != null && 
                       request.auth.uid == resource.data.authorId;
      
      // Suppression : seulement l'auteur peut supprimer
      allow delete: if request.auth != null && 
                       request.auth.uid == resource.data.authorId;
    }
    
    // 🆕 NOUVEAU: Règles pour les favoris partagés (même pattern que journalEntries)
    match /favoriteQuestions/{favoriteId} {
      // Lecture : utilisateur authentifié ET (auteur OU dans partnerIds)
      allow read: if request.auth != null && 
                     (request.auth.uid == resource.data.authorId ||
                      (resource.data.partnerIds != null && 
                       request.auth.uid in resource.data.partnerIds));
      
      // Création : utilisateur authentifié ET auteur ET dans partnerIds
      allow create: if request.auth != null && 
                       request.auth.uid == request.resource.data.authorId &&
                       request.resource.data.partnerIds != null &&
                       request.auth.uid in request.resource.data.partnerIds;
      
      // Modification : seulement l'auteur peut modifier
      allow update: if request.auth != null && 
                       request.auth.uid == resource.data.authorId;
      
      // Suppression : l'auteur OU n'importe qui dans partnerIds peut supprimer
      allow delete: if request.auth != null && 
                       (request.auth.uid == resource.data.authorId ||
                        (resource.data.partnerIds != null && 
                         request.auth.uid in resource.data.partnerIds));
    }
    
    // Règles pour les logs de partage d'abonnement (conformité Apple)
    match /subscription_sharing_logs/{logId} {
      // Permettre la création pour tous les utilisateurs authentifiés (pour les logs)
      allow create: if request.auth != null;
      // Lecture seulement pour les administrateurs (pas implémenté ici)
      allow read: if false;
    }
    
    // Interdire tout autre accès
    match /{document=**} {
      allow read, write: if false;
    }
  }
  
  // 🔧 FONCTION HYBRIDE ULTRA-ROBUSTE - VÉRIFIE TOUS LES CHAMPS POSSIBLES
  function isConnectedPartnerHybrid(currentUserId, targetUserId) {
    let currentUserDoc = get(/databases/$(database)/documents/users/$(currentUserId));
    let targetUserDoc = get(/databases/$(database)/documents/users/$(targetUserId));
    
    return (
      // MÉTHODE 1: Vérification bidirectionnelle classique avec partnerId
      (
        ('partnerId' in currentUserDoc.data && 'partnerId' in targetUserDoc.data) &&
        (currentUserDoc.data.partnerId == targetUserId && 
         targetUserDoc.data.partnerId == currentUserId)
      ) ||
      
      // MÉTHODE 2: Vérification avec connectedPartnerId (ancienne méthode)
      (
        ('connectedPartnerId' in currentUserDoc.data && 'connectedPartnerId' in targetUserDoc.data) &&
        (currentUserDoc.data.connectedPartnerId == targetUserId && 
         targetUserDoc.data.connectedPartnerId == currentUserId)
      ) ||
      
      // MÉTHODE 3: Vérification croisée (partnerId vers connectedPartnerId)
      (
        ('partnerId' in currentUserDoc.data && 'connectedPartnerId' in targetUserDoc.data) &&
        (currentUserDoc.data.partnerId == targetUserId && 
         targetUserDoc.data.connectedPartnerId == currentUserId)
      ) ||
      
      // MÉTHODE 4: Vérification croisée inverse (connectedPartnerId vers partnerId)
      (
        ('connectedPartnerId' in currentUserDoc.data && 'partnerId' in targetUserDoc.data) &&
        (currentUserDoc.data.connectedPartnerId == targetUserId && 
         targetUserDoc.data.partnerId == currentUserId)
      )
    );
  }
} 